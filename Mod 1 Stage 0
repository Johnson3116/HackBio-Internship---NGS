#Biocomputing TASKS

#1.	Name
echo Johnson Nweke #my name

#2.	Creating Directories
mkdir -p Johnson #create a folder titled Johnson

#3. New directory/change
mkdir -p Johnson/biocomputing #create a new directory named biocomputing
cd Johnson/biocomputing #change to the directory

#4. Download these 3 files:
wget https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.fna
wget https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.gbk
wget https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.gbk

#5. Move the .fna file to the folder titled your name
mv https://raw.githubusercontent.com/josoga2/dataset-repos/main/wildtype.fna Johnson/

#6. Delete the duplicate gbk file
Sort -u link.txt



#PROJECT TWO: Installing Bioinformatics Packages with apt-get
sudo apt-get update

#Upgrade existing packages
sudo apt-get upgrade

#Search for Packages
apt-cache search samtools
apt-cache search blast

#Install Common Bioinformatics Tools
sudo apt-get install samtools
sudo apt-get install bcftools
sudo apt-get install bedtools
sudo apt-get install bowtie2
sudo apt-get install hisat2
sudo apt-get install ncbi-blast+
sudo apt-get install emboss
sudo apt-get install hmmer
sudo apt-get install fastqc

#Installing Multiple Packages at Once
sudo apt-get install samtools bcftools bedtools bowtie2 hisat2 fastqc

#Verify Installation
samtools --version #After installation, check the tool’s version: If it prints a version number, it’s installed and ready.

#Upgrading Installed Packages
sudo apt-get upgrade

#Removing a Package
#sudo apt-get remove <package-name>; Example: sudo apt-get remove vim

#Completely Remove (with configuration files): sudo apt-get purge <package-name>

#Clean Up Unused Packages: This removes unnecessary packages that were installed as dependencies but are no longer needed.
sudo apt-get autoremove


#PROJECT TWO: INSTALLATION TASK








#GITHub link: https://github.com/Johnson3116/HackBio-Internship---NGS/edit/main/Mod%201%20Stage%200

#LinkedIn link: https://www.linkedin.com/in/johnson-nweke-medical-lab/




